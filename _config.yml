# Jekyll Configuration
title: "Consent status"
baseurl: ""
url: "https://consent.nathanjn.com"

# GitHub Pages settings
markdown: kramdown
highlighter: rouge
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Font configuration
fonts:
  preconnect_urls:
    - "https://fonts.googleapis.com"
    - "https://fonts.gstatic.com"
  font_urls:
    - "https://fonts.googleapis.com/css2?family=Righteous&family=Monoton&display=swap"

# Consent Status Configuration
# Change this value to update the consent status
# Valid values: "given" or "withdrawn"
consent_status: "withdrawn"

# Dynamically populated when GH runner builds site
last_updated: ""
