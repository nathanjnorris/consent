# Jekyll Configuration
title: "Consent"
description: "Current consent status display"
baseurl: ""
url: "https://consent.nathanjn.com"

# GitHub Pages settings
markdown: kramdown
highlighter: rouge
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Consent Status Configuration
# Change this value to update the consent status
# Valid values: "given" or "withdrawn"
consent_status: "given"

# Dynamically populated when GH runner builds site
last_updated: ""
